"""First-party shared helpers for the repo's CLI utilities.

These modules are imported by the executable scripts in ``bin/`` (each
entry script prepends ``<repo>/src`` to ``sys.path`` before importing
``common``). They carry no shebang and are never executed directly.
"""
